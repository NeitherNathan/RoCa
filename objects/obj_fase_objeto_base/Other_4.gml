//show_debug_message(object_get_name(object_index) + " || " + string(in_sequence) + " || " + string(x) + " || " + string(id));
if (in_sequence)
{
	xstart = x;
}
x_room_start = x;
alarm[11] = 2; // Teste bug